// search的小仓库 实现模块式开发
// state:仓库存储数据的地方
const state = {
  b:'heihei'
};
// mutations:改变state的唯一手段
const mutations = {
};
// action:处理action,可以书写自己的业务逻辑，还可以处理异步
const actions = {
};
// getters:理解为计算属性，用于简化仓库数据，让组件获取仓库的数据更方便
const getters = {};

export default {
  state,
  mutations,
  actions,
  getters
}